@page "/"
@inject IJSRuntime JS

@using CodingPortfolioWebsite.Pages.Components;

<Hero />
<About />
<TechStack />
<ClientProjects />
<PersonalProjects />
<Contact />

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        try
        {
            if (firstRender)
            {
                await JS.InvokeVoidAsync("forceScrollTopOnRefresh");
                await Task.Yield();
                await JS.InvokeVoidAsync("initScrollAnimations");
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error initializing scroll animations: {ex.Message}");

        }
    }
}